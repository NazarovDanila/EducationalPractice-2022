<Page x:Class="BeautiesShopApp.Pages.PageAddProduct"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BeautiesShopApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAddProduct">

    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Фотография товара: "
                       Style="{StaticResource mainTxtb}"/>
                <TextBox Name="CmbxOwner"
                      Style="{StaticResource dfltRb}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Выберете производителя: "
                       Style="{StaticResource mainTxtb}"/>
                <ComboBox Name="CmbxOwner"
                      Style="{StaticResource dfltRb}"/>
            </StackPanel>
            <Button Name="BtnSave" 
                Style="{StaticResource mainBtn}" 
                Content="Сохранить"/>
        </StackPanel>
    </Grid>
</Page>
